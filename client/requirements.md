## Packages
(none needed, all required packages like framer-motion, lucide-react, and date-fns are already in the base stack)

## Notes
- Using JetBrains Mono font to match the terminal/developer aesthetic from the user's HTML.
- Implemented simulated agent delay before triggering the actual `POST /api/receipts` endpoint.
- Included an auto-updating "Past Transactions" list that pulls from `GET /api/receipts`.
